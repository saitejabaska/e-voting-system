<html>
  <head>
    <title>Online Voting Platform</title>
    <meta name="csrf-token" content="<%= csrf %>" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.1/css/all.min.css"
      integrity="sha512-MV7K8+y+gLIBoVD59lQIYicR65iaqukzvf/nwasF0nqhPay5w/9lJmVM2hMDcnK1OnMGCdVK+iQrJ7lzPJQd1w=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />
  </head>
 
<body style="background-color: white;">
  <%- include ('updatedHeader', {username: username}) %>
    

  
    <div class="flex flex-col justify-center items-center p-4">
      <div class="flex flex-col justify-center md:w-2/5 w-full">
        <hr class="mt-2" />
        <div class="pt-6">
          <p class="font-bold text-3xl" style="text-align:center;">New Election</p>
          <div class="flex justify-center">
            <form
              action="/election"
              method="post"
              class="pt-8 flex flex-col gap-4 justify-center items-center"
            >
              <input type="hidden" name="_csrf" value="<%= csrf%>">
              <div class="flex flex-col w-full">
                <label for="name">Name</label>
                <input
                  type="text"
                  name="name"
                  placeholder="Election Name"
                  class="border-2 border-black p-2 rounded-lg"
                />
              </div>
              <button
                type="submit"
                class="bg-green-500 hover:bg-green-700 font-bold border-2 border-green-800 text-white p-2 rounded-lg flex justify-center px-6"
              >
                Add Election
              </button>
            </form>
          </div>
        </div>
      </div>

      <!-- flash message -->
      <div class="flex flex-col justify-center items-center gap-3 pt-4">
        <% if (messages && messages.error && messages.error.length > 0) { %>
          <% for(var i=0; i<messages.error.length; i++) {%>
            <div class="bg-red-400 text-red-800 font-semibold p-2 rounded-lg shadow"> <%= messages.error[i] %></div>
          <% } %>
        <% } %>
      </div>
    </div>
  </body>
</html>
